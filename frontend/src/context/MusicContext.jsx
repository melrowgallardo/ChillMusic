import { PlayerProvider, usePlayer, useMusicContext } from './PlayerContext';

export const MusicProvider = PlayerProvider;
export const useMusic = useMusicContext;
export { PlayerProvider, usePlayer, useMusicContext };
export default PlayerProvider;
